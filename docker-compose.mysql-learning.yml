# This file is for learning only. Do not run it directly.
# It compares Docker MySQL and local MySQL with database-per-service.

# DATABASE-PER-SERVICE NAMES
# Identity Service database: identity_service
# Catalog Service database:  catalog_service

# ------------------------------------------------------------
# LOCAL MYSQL ONLY EXAMPLE
# ------------------------------------------------------------
# services:
#   identity-api:
#     extra_hosts:
#       - "host.docker.internal:host-gateway"
#     environment:
#       ConnectionStrings__Identity: Server=host.docker.internal;Port=3306;Database=identity_service;User=khuntunlar;Password=<password>;
#     depends_on: []
#
#   catalog-api:
#     extra_hosts:
#       - "host.docker.internal:host-gateway"
#     environment:
#       ConnectionStrings__Catalog: Server=host.docker.internal;Port=3306;Database=catalog_service;User=khuntunlar;Password=<password>;
#     depends_on: []

# ------------------------------------------------------------
# DOCKER MYSQL ONLY EXAMPLE
# ------------------------------------------------------------
# services:
#   identity-mysql:
#     image: mysql:8.4
#     environment:
#       MYSQL_DATABASE: identity_service
#     ports:
#       - "3307:3306"
#     volumes:
#       - identity_mysql_data:/var/lib/mysql
#
#   catalog-mysql:
#     image: mysql:8.4
#     environment:
#       MYSQL_DATABASE: catalog_service
#     ports:
#       - "3308:3306"
#     volumes:
#       - catalog_mysql_data:/var/lib/mysql
#
#   identity-api:
#     environment:
#       ConnectionStrings__Identity: Server=identity-mysql;Port=3306;Database=identity_service;User=khuntunlar;Password=<password>;
#
#   catalog-api:
#     environment:
#       ConnectionStrings__Catalog: Server=catalog-mysql;Port=3306;Database=catalog_service;User=khuntunlar;Password=<password>;

# CURRENT COMMANDS
# Docker MySQL mode:
#   docker compose up --build
#
# Local MySQL mode:
#   docker compose -f docker-compose.yml -f docker-compose.local-mysql.yml up --build
